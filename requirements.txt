# バージョンを完全固定している理由:
#   このファイルは GitHub Actions 上で `pip install` され、その実行環境には
#   楽天のアクセスキーが Secret として渡っている。範囲指定(>=)のままだと
#   上流パッケージが改ざんされた新バージョンをCIが自動で取り込み、
#   Secret に到達され得る。
#
# 更新するときは .github/dependabot.yml が出すPRを取り込むこと。
# 手で上げる場合は既知の脆弱性を踏まないよう最新版を確認してから固定する。

# --- 直接依存 ---
requests==2.34.2
Jinja2==3.1.6
PyYAML==6.0.3
python-dotenv==1.2.2

# --- 推移的依存(これらも固定しないと範囲指定で入ってくる) ---
certifi==2026.7.22
charset-normalizer==3.4.9
idna==3.18
MarkupSafe==3.0.3
urllib3==2.7.0
